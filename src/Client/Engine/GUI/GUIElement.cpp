#include "GUIElement.hpp"
#include <iostream>

void GUIElement::updateRelativePosition(const glm::vec2& _windowSize){
    // Checks if absolutelyPositioned is true in which case will update the destRect
    std::cout << "Updating relative position" << std::endl;
}
